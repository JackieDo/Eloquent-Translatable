<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Fallback locale
    |--------------------------------------------------------------------------
    |
    | If a translation has not been set for a given locale, use this locale instead.
    |
    */

    'fallback_locale' => config('app.fallback_locale'),

    /*
    |--------------------------------------------------------------------------
    | Fallback value
    |--------------------------------------------------------------------------
    |
    | Even if the translation for the fallback locale does not exist, we use this
    | value instead.
    |
    */

    'fallback_value' => null

];
